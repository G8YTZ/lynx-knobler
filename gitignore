# macOS
.DS_Store

# Arduino IDE / PlatformIO build artifacts
build/
.pio/
*.hex
*.bin
*.elf
# ...except the merged binary published for browser flashing — this one
# is a real distributable asset, not a build-folder throwaway.
!knobler_merged.bin

# Editor cruft
.vscode/
*.swp

# Anything with a real IP/hostname or credentials in it — see
# knobler.ino's LYNX_HOST placeholder before committing a working copy
*_local.ino
*.local.*
